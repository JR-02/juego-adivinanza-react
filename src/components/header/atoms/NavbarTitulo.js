import styled from 'styled-components'
import theme from '../../theme'

const NavbarTitulo = styled.h1`
  text-align: center;
  color: ${theme.colors.primary};
  text-transform: uppercase;
  margin: 0;
`

export default NavbarTitulo
