import styled from 'styled-components'
import theme from './../../theme'

const ContadorTitulo = styled.h3`
    font-size: 12px;
    color: ${theme.colors.accent};
    text-transform: uppercase;
`

export default ContadorTitulo
