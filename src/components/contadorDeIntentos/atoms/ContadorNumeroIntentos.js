import styled from 'styled-components'
import theme from '../../theme'

const ContadorNumeroIntentos = styled.div`
    font-size: 24px;
    font-weight: bold;
    color: ${theme.colors.secondary};
`

export default ContadorNumeroIntentos
